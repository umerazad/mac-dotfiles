# Session related aliases

alias tls='tmux list-sessions'
alias tns='TERM=screen-256color-bce tmux -2 new -s'
alias tat='tmux attach -d -t'

# ruby-devenv through tmuxinator
alias rdev='tmuxinator ruby-devenv'
